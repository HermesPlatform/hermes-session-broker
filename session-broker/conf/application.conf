# https://www.playframework.com/documentation/latest/Configuration
play.server.pidfile.path=/dev/null
# play.http.secret.key="this_is_a_secret_hopefully_not_too_short"

slick.dbs.default {
  profile = "slick.jdbc.PostgresProfile$"
  db {
    driver = "org.postgresql.Driver"
    url="jdbc:postgres://localhost:5432/hermes"
    user=postgres
    password=postgres
    numThreads=20
    maxConnections=20
  }
}